import React from 'react';
import './styles.scss'

const Portfolio = props => {
    return (
        <section className="homepage">
            Portfolio
        </section>
    );
};

export default Portfolio;