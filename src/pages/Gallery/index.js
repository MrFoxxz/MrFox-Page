import React from 'react';
import './styles.scss'

const Gallery = props => {
    return (
        <section className="homepage">
            Gallery
        </section>
    );
};

export default Gallery;