import React from 'react';
import './styles.scss'

const Homepage = props => {
    return (
        <section className="homepage">
            Homepage
        </section>
    );
};

export default Homepage;